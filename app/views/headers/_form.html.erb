<center>
<%= form_for(@header) do |f| %>
  <% if @header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@header.errors.count, "error") %> prohibited this header from being saved:</h2>

      <ul>
      <% @header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :heading %><br />
    <%= f.text_field :heading %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.collection_select(:course_id, Course.all, :id , :name) %>
  </div>
  <div class="field">
    <%= f.label :syllabus_id %><br />
    <%= f.collection_select(:syllabus_id, Syllabus.all, :id , :name) %>
  </div>
  <div class="field">
    <%= f.label :chapter_id %><br />
    <%= f.collection_select(:chapter_id, Chapter.all, :id , :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</center>